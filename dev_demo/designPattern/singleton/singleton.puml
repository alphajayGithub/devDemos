@startuml

Client --> Singleton: Singleton*=Singleton::GetInstance()

class Singleton{
    # {static} Singleton*
    # value
    # Singleton(value)
    + <del>Singleton(&) </del>
    + <del>Singleton=(&) </del>
    + {static} GetInstance(): Singleton
}

@enduml